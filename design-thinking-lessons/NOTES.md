# Notes

- User is a senior software engineer. Never teach syntax. Teach vocabulary + "why" + tradeoffs.
- Core stated problem: forgets terminology/key points/ideas after studying. Retention (storage strength) is the design constraint for every lesson, not exposure (fluency).
- Explicit ask: flag when a concept is used correctly but named wrong, or named right but used wrong. Build this into every explain-back checkpoint.
- Gate progression on demonstrated understanding: if an explanation is shaky/fuzzy, stay on the topic — "covered" is not the same as "learned."
- True spaced recall needs elapsed real time between sessions, which a single session can't simulate. Plan: open every subsequent session with a quick recall check on the last 1-2 lessons' vocabulary before introducing new material.
- Primary-track order (fixed by user): (1) Component/API design → (2) State management → (3) Rendering strategy → (4) Performance/scalability → (5) TBD, decided from how the first four land — leading candidate is a capstone lesson that generalizes coupling/cohesion/abstraction/tradeoff-analysis outside frontend, per the user's requested narrow-then-broad structure.
- Broadening pass: user wants a full narrow (Next.js-specific) pass through all 4 topics first, THEN a second pass broadening each into the general software-design principle. Don't broaden prematurely inside lesson 1 — plant one-line forward references only.
- Secondary track (code review, testing, incident response, design docs) is lower-intensity and should attach to whatever primary-track topic makes it natural — not run on its own schedule.
- Next.js moves fast: verify official docs before teaching rendering-strategy content. Confirmed Next.js 16 (2026-05-13 docs snapshot) uses Cache Components (`use cache`, Partial Prerendering, Suspense streaming) as the current model — do not default to older ISR/SSG-only framing from training data.
- Quiz format constraint: multiple-choice answers must be near-equal length/word-count so formatting gives no clue.
- **2026-07-15 pacing change:** user asked for all lessons written up front rather than gated one-at-a-time. Given explicit choice between (a) outline-now-write-later, (b) write-all-but-track-checkpoints-separately, (c) write-all-drop-the-gate — user chose (c). All 9 lessons (4 narrow + 1 added + 4 broadening) are now written and available with no lock. This trades some retention rigor for flexibility — the active-retrieval mechanics (quizzes, build-then-explain exercises, checkpoints) are still embedded in each lesson, but nothing blocks moving on without doing them. Compensate by being more assertive in conversation about actually running the spaced-recall checks later, since the lessons alone won't enforce it anymore.
- Because content for all lessons now exists before any conversation-based understanding-check has happened, GLOSSARY.md and learning-records/ are still empty by design — those only get populated once the user actually demonstrates understanding in conversation, regardless of how much lesson content exists on disk.
